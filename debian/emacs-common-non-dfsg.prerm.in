#! /bin/sh

set -e

case "$1" in
  remove|deconfigure)
    update-alternatives --remove emacs.info.gz \
                        /usr/share/info/emacs-@MAJOR_VERSION@/emacs.info.gz
    ;;
esac

#DEBHELPER#
